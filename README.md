
  # Cafe POS UI Design

  This is a code bundle for Cafe POS UI Design. The original project is available at https://www.figma.com/design/ktaPoDTofwTOQld1Pp6lWr/Cafe-POS-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  